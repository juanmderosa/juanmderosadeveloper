---
import { actions } from "astro:actions";

const { t } = Astro.props;
---

<form
  id="contactame"
  method="POST"
  data-submit-label={t.contact.form.submit}
  data-sending-label={t.contact.form.sending}
  action={actions.sendEmail}>
  <div class="input-container">
    <label
      for="nombre"
      class="label"
      >{t.contact.form.name}</label
    >
    <input
      type="text"
      name="nombre"
      class="input-field"
      id="nombre"
      required
    />
    <p
      id="error-nombre"
      class="input-text-error">
    </p>
  </div>

  <div class="input-container">
    <label
      for="email"
      class="label"
      >{t.contact.form.email}</label
    >
    <input
      type="email"
      name="email"
      class="input-field"
      id="email"
      required
    />
    <p
      id="error-email"
      class="input-text-error">
    </p>
  </div>

  <div class="input-container">
    <label
      for="telefono"
      class="label"
      >{t.contact.form.phone}</label
    >
    <input
      type="text"
      name="telefono"
      class="input-field"
      id="telefono"
      required
    />
    <p
      id="error-telefono"
      class="input-text-error">
    </p>
  </div>

  <div class="input-container">
    <label
      for="mensaje"
      class="label"
      >{t.contact.form.message}</label
    >
    <textarea
      name="mensaje"
      class="textarea-field"
      id="mensaje"
      required
    ></textarea>
    <p
      id="error-mensaje"
      class="input-text-error">
    </p>
  </div>

  <button
    type="submit"
    id="submitBtn"
    class="button"
    disabled>
    {t.contact.form.submit}
  </button>
</form>

<p
  class="text-error"
  style="display:none;">
  {t.contact.form.error}
</p>

<p
  class="text-confirmation"
  style="display:none;">
  {t.contact.form.success}
</p>

<style>
  #contactame {
    padding: 1.5rem;
    display: flex;
    flex-direction: column;
    gap: 1rem;
    width: 100%;
    align-items: stretch;
  }

  .input-container {
    display: flex;
    flex-direction: column;
  }

  .label {
    margin-bottom: 0.3rem;
    color: var(--on-surface);
    font-family: "JetBrains Mono", monospace;
    font-size: 0.74rem;
    font-weight: 500;
  }

  .input-field,
  .textarea-field {
    background-color: rgba(15, 14, 23, 0.58);
    border: 1px solid rgba(255, 255, 255, 0.12);
    padding-inline: 1rem;
    padding-block: 0.75rem;
    outline: none;
    border-radius: var(--radius);
    width: 100%;
    color: var(--on-surface);
    transition:
      border-color 300ms ease,
      box-shadow 300ms ease;
  }

  .input-field {
    height: 2.7rem;
    font-weight: 500;
  }
  .textarea-field {
    height: 7rem;
    resize: none;
  }

  .input-field:focus,
  .textarea-field:focus {
    border-color: var(--primary-container);
    box-shadow: 0 0 0 3px rgba(109, 84, 207, 0.18);
  }

  .text-error {
    color: var(--error);
    padding-inline: 1rem;
    margin-top: 0.2rem;
    text-align: center;
    display: none;
  }

  .input-text-error {
    color: var(--error);
    padding-inline: 1rem;
    margin-top: 0.2rem;
    font-size: 0.7rem;
  }

  .text-confirmation {
    padding-inline: 1rem;
    margin-top: 0.2rem;
    text-align: center;
    color: var(--primary);
    display: none;
  }

  #submitBtn:disabled {
    opacity: 0.5;
    cursor: not-allowed;
  }

  @media screen and (max-width: 768px) {
    #contactame {
      padding: 1rem;
    }
  }
</style>

<script>
  import { actions } from "astro:actions";
  import { mapZodIssues, isInputError } from "../helpers/mapZodIssues";
  declare const grecaptcha: {
    execute: (key: string, options: { action: string }) => Promise<string>;
  };

  const requiredFields = ["nombre", "email", "telefono", "mensaje"] as const;

  const form = document.getElementById("contactame") as HTMLFormElement | null;
  const submitBtn = document.getElementById(
    "submitBtn",
  ) as HTMLButtonElement | null;

  const errorMessage = document.querySelector(
    ".text-error",
  ) as HTMLParagraphElement | null;
  const confirmationMessage = document.querySelector(
    ".text-confirmation",
  ) as HTMLParagraphElement | null;

  function clearErrors() {
    requiredFields.forEach((field) => {
      const el = document.getElementById(
        `error-${field}`,
      ) as HTMLParagraphElement | null;
      if (el) el.textContent = "";
    });
  }

  function applyFieldErrors(errors: Record<string, string[]>) {
    Object.entries(errors).forEach(([field, messages]) => {
      const el = document.getElementById(
        `error-${field}`,
      ) as HTMLParagraphElement | null;
      if (el) el.textContent = messages.join(",");
    });
  }

  if (form && submitBtn && errorMessage && confirmationMessage) {
    form.addEventListener("submit", async (e: SubmitEvent) => {
      e.preventDefault();

      clearErrors();
      errorMessage.style.display = "none";
      confirmationMessage.style.display = "none";

      const formData = new FormData(form);

      submitBtn.disabled = true;
      submitBtn.textContent = form.dataset.sendingLabel ?? "Enviando...";

      try {
        const token = await grecaptcha.execute(
          import.meta.env.PUBLIC_RECAPTCHA_KEY,
          { action: "submit" },
        );

        formData.append("recaptchaToken", token);

        const { data, error } = await actions.sendEmail(formData);

        if (error) {
          if (isInputError(error)) {
            const fieldErrors = mapZodIssues(error.issues);
            applyFieldErrors(fieldErrors);
          } else {
            errorMessage.style.display = "block";
          }
          return;
        }

        if (data?.success) {
          form.reset();
          confirmationMessage.style.display = "block";

          setTimeout(() => {
            confirmationMessage.style.display = "none";
          }, 5000);
        }
      } catch (err) {
        errorMessage.style.display = "block";
      } finally {
        submitBtn.disabled = false;
        submitBtn.textContent = form.dataset.submitLabel ?? "Send";
      }
    });

    form.addEventListener("input", () => {
      const allFilled = requiredFields.every((field) => {
        const input = document.getElementById(field) as
          | HTMLInputElement
          | HTMLTextAreaElement
          | null;
        return input?.value.trim() !== "";
      });

      submitBtn.disabled = !allFilled;
    });
  }
</script>
